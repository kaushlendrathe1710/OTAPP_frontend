import React from "react";

const ChatMessagesList = ({ data }) => {
  return (
    <div>
      {/* {data.map((item, i) => {
        return <AdminAdminMessage message={item} />;
      })} */}
    </div>
  );
};

export default ChatMessagesList;
