import React from 'react';

const SelectionRect = ({selectionRectStyle}) => {
    return (
        <rect width="300" height="100" style={selectionRectStyle} />
    );
}

export default SelectionRect;
